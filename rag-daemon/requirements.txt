# ============================================================
# Hybrid RAG Edge Daemon — Dependencies
# ============================================================
# Install with: pip install -r requirements.txt
#
# Python 3.11+ is required.
#
# Do NOT install aider-chat in this venv — it requires huggingface-hub==1.4.1
# which conflicts with sentence-transformers. Install aider via pipx instead:
#   pipx install aider-chat
# ============================================================

# --- Web Framework -------------------------------------------------------
fastapi>=0.111.0,<0.200.0          # Async HTTP server framework
uvicorn[standard]>=0.29.0,<0.30.0  # ASGI server (includes websockets, watchfiles)

# --- Data Validation & Settings ------------------------------------------
pydantic>=2.7.0,<3.0.0             # Data models and validation
pydantic-settings>=2.2.0,<3.0.0    # .env / config.json loading via BaseSettings

# --- Vector Database -----------------------------------------------------
chromadb>=0.5.0,<0.6.0             # Local persistent vector store

# --- Embeddings ----------------------------------------------------------
sentence-transformers>=3.0.0,<4.0.0  # Local embedding models (all-MiniLM-L6-v2)
transformers>=4.41.0,<5.0.0         # Pinned: sentence-transformers requires 4.x

# --- Sparse Search (BM25) ------------------------------------------------
rank-bm25>=0.2.2,<0.3.0            # BM25 sparse retrieval for hybrid search

# --- Utilities -----------------------------------------------------------
python-dotenv>=1.0.0,<2.0.0        # .env file support (used by pydantic-settings)
requests>=2.31.0,<3.0.0            # HTTP client for Spotify Web API
markdown>=3.5.0,<4.0.0             # Markdown to HTML for research PDF
xhtml2pdf>=0.2.13,<0.3.0           # HTML to PDF (pure Python)

# --- Host Worker ---------------------------------------------------------
redis>=4.6.0,<6.0.0                # Redis client for host_worker.py (raw TCP BLPOP)
psutil>=5.9.0,<7.0.0               # System metrics for system_status action
pycaw>=20181226                    # Windows Core Audio WASAPI wrapper (lockdown mute)
pywin32>=306                       # win32gui/win32process for visible-window enumeration (list_apps)
screen-brightness-control>=0.23.0  # DDC/CI + WMI brightness for adjust_brightness
